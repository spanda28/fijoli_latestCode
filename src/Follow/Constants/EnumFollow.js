
const EnumFollow = Object.freeze({
    followers  : "followers",
    following  : "following",
    none       : "none"
})

// export default EnumFollow;


//enum type already exists in enumnavigate - not necessary to use this
